node {
    stage("CleanWorkspace") {
        sh "rm -r *"
    }
    stage("CloneRepos") {
        sh '''

        '''
    }
    stage("Build") {
        sh '''
        ls
        tree .
        '''
    }
    stage("Artifact") {
        sh '''

        '''
    }
}
